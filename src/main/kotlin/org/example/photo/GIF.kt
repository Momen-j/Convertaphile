package org.example.photo

import org.example.utilities.ConvertableFileType

class GIF(override val inputFilePath: String): ConvertableFileType {
    // override convertTo function in case I need to specify flags
}