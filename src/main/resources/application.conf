ktor {
    application {
        modules = [ org.example.ApplicationKt.module ]  # Update this to match your actual module path
    }

    deployment {
        port = 8080
        port = ${?PORT}  # This allows Railway to override the port via environment variable
    }
}